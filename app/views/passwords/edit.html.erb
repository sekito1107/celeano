<div class="auth-page">
  <%= render AuthLogoComponent.new %>

  <%= form_with url: password_path(params[:token]), method: :put, class: "auth-form" do |form| %>
    <%= render AuthFormHeaderComponent.new(title: "Forge New Key") %>

    <%= render FormInputComponent.new(
      form: form,
      field: :password,
      label: "New Passphrase",
      icon: "🔑",
      type: :password,
      required: true,
      autocomplete: "new-password",
      placeholder: "••••••••",
      maxlength: 72
    ) %>

    <%= render FormInputComponent.new(
      form: form,
      field: :password_confirmation,
      label: "Confirm Passphrase",
      icon: "🔑",
      type: :password,
      required: true,
      autocomplete: "new-password",
      placeholder: "••••••••",
      maxlength: 72
    ) %>

    <%= render ButtonComponent.new(text: "Update Credentials") %>

    <div class="auth-links auth-links--centered">
      <%= link_to "Return to Archive", new_session_path %>
    </div>
  <% end %>
</div>
