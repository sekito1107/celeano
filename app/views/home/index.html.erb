<% content_for :head do %>
  <%= stylesheet_link_tag "home", "data-turbo-track": "reload" %>
<% end %>

<div class="lp-container">
  <main class="lp-content">
    <p class="lp-subtitle">プレアデス星団の彼方へ</p>
    <h1 class="lp-title">
      <span class="title-prefix">CALL OF</span>
      <span class="title-main">CELEANO</span>
    </h1>
    
    <div class="lp-description">
      <p>禁断の知識、石版の記憶。</p>
      <p>深宇宙の図書館で、あなたの<span class="sanity-trigger" data-alt="狂気">正気</span>は試される。</p>
    </div>

    <div class="lp-actions">
      <% if authenticated? %>
        <%= link_to "探索を再開する", lobby_path, class: "lp-button" %>
      <% else %>
        <%= link_to new_session_path, class: "lp-button" do %>
          <span class="icon">❖</span> 探索を開始する
        <% end %>
      <% end %>
    </div>
  </main>
</div>
