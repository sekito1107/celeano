<div class="auth-page">
  <%= render AuthLogoComponent.new %>

  <%= form_with url: session_path, class: "auth-form" do |form| %>
    <%= render AuthFormHeaderComponent.new(title: "Investigator Login", sanity_indicator: "Sanity Stable") %>

    <%= render FormInputComponent.new(
      form: form,
      field: :email_address,
      label: "Identifier",
      icon: "👤",
      type: :email,
      required: true,
      autofocus: true,
      autocomplete: "username",
      placeholder: "your@email.com",
      value: params[:email_address]
    ) %>

    <%= render FormInputComponent.new(
      form: form,
      field: :password,
      label: "Passphrase",
      icon: "🔑",
      type: :password,
      required: true,
      autocomplete: "current-password",
      placeholder: "••••••••",
      maxlength: 72
    ) %>

    <div class="auth-links">
      <label class="auth-remember">
        <%= form.check_box :remember_me, class: "auth-remember__checkbox" %> Keep Gate Open
      </label>
      <%= link_to "Lost Knowledge?", new_password_path %>
    </div>

    <%= render ButtonComponent.new(text: "Access Archive") %>
    
    <div class="auth-attribution">
      Miskatonic University Network
    </div>
  <% end %>

  <div class="auth-footer">
    <%= link_to "Request New Clearance", new_registration_path, class: "btn-text" %>
  </div>
</div>
