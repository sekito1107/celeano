<!DOCTYPE html>
<html lang="ja">
  <head>
    <title><%= content_for(:title) || "Celaeno" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Celaeno">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Google Fonts - Shippori Mincho & Klee One & Cinzel Decorative %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cinzel+Decorative:wght@400;700;900&family=Klee+One&family=Shippori+Mincho:wght@400;500;600&display=swap" rel="stylesheet">

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%# Explicitly load stylesheets to ensure Propshaft finds them and order is correct %>
    <%= stylesheet_link_tag "application", "animations", "phase_transitions", "madness_glow", "shatter_effect", "card", "game_board", "home", "lobby", "matchmaking", "status_bar", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="<%= controller_path.gsub('/', '-') %>-<%= action_name %>">
    <%= render FlashComponent.new(flash: flash) %>
    <%= yield %>
  </body>
</html>

